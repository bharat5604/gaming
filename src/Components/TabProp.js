import React from "react";

const TabProp = (props) => {
  return (
    <div>
      <div className="images">
        <img src={props.games} alt="" />
      </div>
    </div>
  );
};

export default TabProp;
